name: Build Kernel Module

on:
  push:
    branches: [ master ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    uses: sfera-labs/sl-kmod-commons/.github/workflows/kmod-build.yml@main
    with:
      archs: '["arm64","armhf"]'
      kernel_count: 3
      module_path: '.'
